import styled from 'styled-components';

export const BoxRespon = styled.div`
      width: 35%;
    height: 35.5vh;
    background: var(--white);
    border: 0.5vh solid var(--black);
    border-radius: 1vh;
    font-size: 8vh;
    margin-left: 34.5%;
    text-align: center;
    padding-top: 4%;

  #titulo {
    color: var(--black);
  }
`;
